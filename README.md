# py-iris-ml-project
Python ML project using Iris dataset from UCI.

## Setup
1. Install dependencies: `pip install -r requirements.txt`
2. Run `python download-data.py` to get data.
3. Run `python clean-data.py` to preprocess.
3. Run `python train-model.py` to train model.
